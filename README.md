
## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

## Deploying to GitHub Pages

1. Push this project to a GitHub repository.
2. In GitHub, enable GitHub Pages and select the `gh-pages` branch.
3. The included workflow will build and publish the site automatically on every push to the `main` branch.
  